services:
  denotary-db-agent:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: denotary-db-agent
    restart: unless-stopped
    volumes:
      - ../examples/agent.example.json:/app/config/agent.json:ro
      - denotary-db-agent-state:/data
    command:
      - --config
      - /app/config/agent.json
      - run
      - --interval-sec
      - "5"

volumes:
  denotary-db-agent-state:
